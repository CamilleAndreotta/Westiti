api: 
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
    
providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

logs:
  level: "DEBUG"
  filePath: "/var/log/traefik.log"
  format: "json"
  maxSize: 1
  maxBackups: 3



